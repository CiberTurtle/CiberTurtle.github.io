export type PartyBoxOptions = {
	width: number
	height: number
}


export const party_box = (canvas: HTMLCanvasElement, options: PartyBoxOptions) => {
	const ctx = canvas.getContext('2d') as CanvasRenderingContext2D

	return {}
}
